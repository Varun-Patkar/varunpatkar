"use client";

import ResumeView from "@/components/resume/ResumeView";

/**
 * /resume route — renders the interactive resume page.
 * This page is independent of the portfolio theme.
 */
export default function ResumePage() {
	return <ResumeView />;
}
